import 'package:flutter/material.dart';

class Esqueci extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Esqueci a Senha"),
        centerTitle: true,
        backgroundColor: Colors.lightBlue,
      ),
      body: Container(),
    );

  }
}
